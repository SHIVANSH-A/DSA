Binary Search 
Searching when the data is sorted.

Code - i)Iterative
        f(a,target){
            int n = a.size();
            int l=0,r=n-1;

            while(l<=r){
                int mid = l + (r-l)/2;

                if(a[mid]==taregt) return mid;
                else if(target<a[mid]) r = mid-1;
                else l = mid +1;
            }

            return -1;
        }